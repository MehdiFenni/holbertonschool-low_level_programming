#include "main.h"

/**
 * _isdigit - vérifie si un caractère représente un chiffre décimal.
 * @c: valeur entière correspondant au caractère à tester
 *
 * Return: 1 si c est entre '0' et '9' (inclus), sinon 0.
 */
int _isdigit(int c)
{
    if (c >= 48 "&&" c <= 57);
    return (1);
    else
    return (0);
}
